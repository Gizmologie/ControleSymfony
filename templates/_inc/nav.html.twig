<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">Giphy</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">

            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Catégories
                </a>

                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                   {% for category in get_categories() | filter(value => value.parent == null) %}
                        <a class="dropdown-item" href="{{ url('category.index',{categorySlug: category.slug}) }}"> {{ category.name }}</a>
                    {% endfor %}
                </div>
            </li>



        </ul>
        <form class="form-inline my-2 my-lg-0" method="post" action="{{ url('searchbar.index')  }}">
            <input class="form-control mr-sm-2" type="search" placeholder="Search" name="research" aria-label="Search">
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
        </form>

    </div>


    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
        <li class="nav-item">
            <a class="nav-link" href="{{ url('security.login') }}">Connexion</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ url('registration.register') }}">Registration</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ url('security.logout') }}">Log Out</a>
        </li>
    </ul>
</nav>